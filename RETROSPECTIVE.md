# RETROSPECTIVE

## 2026-05-25 - 관리자 메뉴 정리

- 사용자 요청 요약: 관리자 쪽의 `홈페이지 설정` 항목은 더 이상 필요 없어 보이므로 제거 요청.
- 진행한 문제: 관리자 내 설정 페이지 구조에서 홈페이지 설정 진입점과 관련 UI 잔여물을 정리.
- 확인한 원인과 단서: `AdminNav`에는 이미 홈페이지 설정 메뉴가 제거되어 있었고, 삭제된 라우트의 빈 폴더와 미사용 CSS가 남아 있었다.
- 시도한 해결 과정: `homepage`, `홈페이지`, `admin-photo-preview` 관련 참조를 검색하고 남은 CSS와 빈 라우트 폴더를 제거했다.
- 적용한 변경 사항: `app/admin/homepage` 제거, 관리자 사진 미리보기 CSS 제거, 홈페이지 설정 메뉴/모드 참조 없음 확인.
- 검증 결과: `npx tsc --noEmit` 통과. 삭제된 라우트 때문에 남아 있던 `.next/types/app/admin/homepage` 캐시는 제거 후 재검증했다.
- 남은 위험 요소: `.next` 캐시는 개발 서버 실행 상태에 따라 다시 꼬일 수 있으므로, 라우트 삭제 후 이상하면 개발 서버 재시작이 필요하다.
- 다음 단계: 실제 브라우저에서 관리자 페이지 탭 이동이 의도대로 보이는지 확인하면 된다.

## 2026-05-25 - 예약 기록/메뉴 운영 UX 수정

- 사용자 요청 요약: `전체 기록`은 진행 중과 처리 기록이 한 화면 안에서 나뉘어 보이는 이전 방식이 더 좋고, 메뉴 운영의 품절/숨김 체크박스는 더 크게 보이며 저장 버튼 없이 즉시 적용되길 원함.
- 진행한 문제: 예약 목록의 전체 기록 표시 방식과 메뉴 운영 저장 흐름을 조정.
- 확인한 원인과 단서: 전체 기록 탭이 모든 상태를 하나의 테이블에 섞어 보여 상태 흐름이 덜 명확했고, 메뉴 운영은 체크 후 별도 저장 버튼을 눌러야 해서 조작감이 번거로웠다.
- 시도한 해결 과정: 예약 탭 컴포넌트에서 전체 기록일 때만 `진행 중`과 `처리 기록` 테이블을 분리하고, 메뉴 운영 체크 변경 시 바로 설정 API에 PATCH 하도록 변경했다.
- 적용한 변경 사항: 메뉴 운영의 품절/숨김 체크박스 크기와 터치 영역 확대, 메뉴 운영 페이지 저장 버튼 숨김, 체크 즉시 저장 메시지 표시.
- 검증 결과: `npx tsc --noEmit` 통과.
- 남은 위험 요소: 체크 즉시 저장은 네트워크가 느릴 때 잠깐 비활성화되므로 실제 브라우저에서 클릭 반응감을 확인할 필요가 있다.
- 다음 단계: 로컬 서버 재시작 후 `/admin/reservations`, `/admin/menu`에서 화면 배치와 즉시 저장 동작을 확인한다.

## 2026-05-25 - 예약 완료 문구/번호 표시 개선

- 사용자 요청 요약: 예약 완료 후 UUID가 그대로 보이지 않고 `001` 같은 짧은 예약번호로 보이길 원했고, 완료 문구를 더 부드럽게 변경 요청.
- 진행한 문제: 예약 API 응답에 사용자용 예약번호를 추가하고, 예약 완료 카드의 문구와 줄바꿈 표시를 수정.
- 확인한 원인과 단서: 내부 예약 ID는 UUID로 관리되고 있었고, 프론트 완료 화면에서 이를 그대로 출력하고 있었다.
- 시도한 해결 과정: 저장 구조는 유지하고, 예약 생성 후 같은 날짜의 생성 순서를 계산해 `001` 형식으로 내려주도록 했다.
- 적용한 변경 사항: `reservationNumber` API 응답 추가, 완료 화면 예약번호 표시 변경, 문구를 `예약 요청이 접수되었습니다 : )`로 변경.
- 검증 결과: `npx tsc --noEmit` 통과.
- 남은 위험 요소: 동일 시각 동시 예약이 극단적으로 겹치면 표시 번호 계산이 완전한 DB 시퀀스만큼 엄격하지는 않다.
- 다음 단계: 브라우저에서 새 예약을 넣어 완료 카드에 `001` 형식 번호와 줄바꿈 문구가 보이는지 확인한다.

## 2026-05-25 - PWA/Web Push 알림 테스트 구조 추가

- 사용자 요청 요약: 문자 API 없이 사이트/PWA만으로 휴대폰 예약 알림을 실험할 수 있는지 확인하고, 가능한 부분은 끝까지 구현 요청.
- 진행한 문제: 관리자 휴대폰에서 웹 푸시 구독을 등록하고 새 예약 생성 시 알림을 보내는 기본 구조를 추가.
- 확인한 원인과 단서: 배포 주소가 HTTPS라 PWA/Web Push의 필수 조건은 충족되어 있었고, 문자 API 없이도 VAPID 기반 웹 푸시로 실험 가능했다.
- 시도한 해결 과정: `web-push` 패키지 설치, VAPID 키 생성, 로컬 `.env`에 키 저장, manifest/service worker/API/provider/관리자 버튼을 추가했다.
- 적용한 변경 사항: PWA manifest, `/sw.js`, `/api/push/public-key`, `/api/push/subscriptions`, push subscription 저장소, web-push automation provider, 관리자 알림 설정의 `이 기기에서 알림 받기` UI 추가.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과, 로컬 프로덕션 서버에서 manifest/public-key/service-worker 200 응답 확인.
- 남은 위험 요소: Vercel CLI 로그인이 현재 환경에서 확인되지 않아 배포 환경변수 등록은 아직 직접 완료하지 못했다.
- 다음 단계: Vercel 프로젝트 환경변수에 `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`를 등록하고 재배포한 뒤 휴대폰에서 알림 권한을 테스트한다.

## 2026-05-25 - 관리자 모바일 예약 목록/알림 해제 개선

- 사용자 요청 요약: 모바일에서 예약 목록 오른쪽이 잘려 보이고 관리자 버튼 배치도 모바일에 맞지 않으며, 웹푸시 알림 해제 버튼도 필요함.
- 진행한 문제: 데스크톱 표 중심의 예약 목록을 모바일 카드형으로 병행 표시하고, 관리자 헤더/탭을 모바일 그리드로 재배치.
- 확인한 원인과 단서: 기존 `.admin-table`은 `min-width: 1060px` 표라 모바일에서 가로 스크롤 또는 잘림이 발생했고, 알림 설정 UI에는 구독 등록만 있고 삭제 흐름이 없었다.
- 시도한 해결 과정: 520px 이하에서는 표를 숨기고 카드 리스트를 표시하도록 변경, `/api/push/subscriptions`에 DELETE 추가, 클라이언트에서 `unsubscribe()`와 서버 구독 삭제를 함께 실행하도록 수정.
- 적용한 변경 사항: 예약 카드형 모바일 UI, 관리자 헤더/탭 모바일 레이아웃, `이 기기 알림 해제` 버튼, 알림 해제 API.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과, 로컬 프로덕션 서버에서 `/admin/reservations` 200 응답 확인.
- 남은 위험 요소: Playwright가 현재 런타임에 없어 실제 모바일 스크린샷 자동 검수는 못 했으므로 배포 후 휴대폰에서 여백과 버튼 크기 확인 필요.
- 다음 단계: GitHub 푸시 후 Vercel 배포 완료 시 휴대폰에서 `/admin/reservations`와 `/admin/settings`를 확인한다.

## 2026-05-26 - 예약 날짜 검증 KST 기준 수정

- 사용자 요청 요약: 2026-05-26으로 정상 표시되는데도 예약 제출 시 `예약 날짜는 오늘만 선택할 수 있습니다.` 오류가 발생한다고 제보.
- 진행한 문제: 서버 예약 검증의 오늘 날짜/현재 시간 계산 기준을 확인하고 한국 시간 기준으로 수정.
- 확인한 원인과 단서: Vercel 서버는 UTC 기준으로 동작할 수 있는데, 기존 검증은 `new Date().getFullYear()` 등 서버 로컬 시간 기준으로 오늘을 만들고 있었다. 한국 시간 26일이어도 서버는 25일로 판단할 수 있었다.
- 시도한 해결 과정: `Asia/Seoul` 기준 날짜/시간 유틸을 추가하고 예약 검증 및 홈페이지 영업 상태 표시에서 해당 유틸을 사용하도록 변경했다.
- 적용한 변경 사항: `lib/datetime/korea.ts` 추가, 예약 검증의 today/current time/weekend 계산을 KST 기준으로 변경, 홈페이지 영업 상태도 KST 기준으로 변경.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과, KST 유틸이 `2026-05-26`을 반환하는 것 확인.
- 남은 위험 요소: 클라이언트 휴대폰 시간이 한국 시간이 아닌 경우 화면 표시와 서버 판단이 다를 수 있으나, 한국 카페 예약 서비스 기준으로 서버 KST 판단이 맞다.
- 다음 단계: GitHub 푸시 후 Vercel 배포 완료 시 다시 예약 제출을 확인한다.

## 2026-05-26 - 문자 알림 UI 제거

- 사용자 요청 요약: 웹푸시 알림이 동작하므로 문자 알림 기능의 뿌리는 남기되 관리자 인터페이스에서는 문자 수신 관련 항목을 제거 요청.
- 진행한 문제: 알림 설정 화면에 남아 있던 휴대폰 수신, 문자/SMS 방식 선택, 테스트 수신 문구 UI를 정리.
- 확인한 원인과 단서: `AdminSettingsForm`의 notification 모드가 휴대폰 번호와 문자/SMS 선택지를 렌더링하고 있었고, 알림 설정 페이지에서 해당 폼을 함께 보여주고 있었다.
- 시도한 해결 과정: 알림 설정 페이지를 웹푸시 설정 전용 화면으로 단순화하고, 관리자 설정 폼에서는 영업/휴무와 메뉴 운영 모드만 남겼다.
- 적용한 변경 사항: `/admin/settings`에서 문자 관련 폼 제거, `AdminSettingsForm`의 notification UI 분기 제거, 내부 `NotificationMode` 타입과 저장 구조는 유지.
- 검증 결과: `npm run build` 통과.
- 남은 위험 요소: 내부 자동화 provider 확장용 SMS 타입은 남아 있으므로, 향후 문자 기능을 다시 쓰려면 별도 UI를 새로 붙이면 된다.
- 다음 단계: 배포 후 `/admin/settings`에서 웹푸시 알림 설정만 보이는지 확인한다.

## 2026-05-26 - 지난 예약 자동 처리 완료

- 사용자 요청 요약: 예약 시간이 지나면 자동으로 `처리 완료` 상태로 바뀌게 할 수 있는지 문의.
- 진행한 문제: 별도 스케줄러 없이 관리자 목록/예약 API 조회 시 지난 진행 중 예약을 자동 정리하는 방식으로 구현.
- 확인한 원인과 단서: 현재 예약 목록은 조회 시 DB에서 직접 불러오므로, 조회 직전에 `pending`/`confirmed` 중 날짜와 시간이 지난 예약을 `completed`로 업데이트하면 운영상 자동 처리처럼 동작한다.
- 시도한 해결 과정: 한국 시간 기준 오늘 날짜와 현재 시간을 사용해 `date < today` 또는 `date = today AND time < currentTime`인 진행 중 예약을 일괄 업데이트.
- 적용한 변경 사항: `completeExpiredReservations()` 추가, `listReservations()` 시작 시 자동 호출.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 완전한 백그라운드 작업이 아니라 예약 목록/API가 조회될 때 정리된다. 아무도 관리자를 열지 않으면 그 순간까지 상태 변경은 지연될 수 있다.
- 다음 단계: 배포 후 예약 시간이 지난 항목이 관리자 목록 새로고침 시 처리 기록으로 내려가는지 확인한다.

## 2026-05-26 - 관리자 전체 진입 시 지난 예약 정리

- 사용자 요청 요약: 예약 목록뿐 아니라 관리자 페이지에 들어갈 때마다 지난 예약이 정리되도록 범위를 넓히길 원함.
- 진행한 문제: `/admin` 하위 모든 페이지 진입 시 지난 진행 중 예약을 `처리 완료`로 정리하도록 변경.
- 확인한 원인과 단서: 기존 구현은 `listReservations()` 호출 시에만 실행되어 예약 목록/API 조회 때만 정리됐다.
- 시도한 해결 과정: `app/admin/layout.tsx`를 추가해 관리자 하위 라우트 렌더 전에 `completeExpiredReservations()`를 호출하고, 예약 목록 조회의 중복 호출은 제거했다.
- 적용한 변경 사항: 관리자 레이아웃 자동 정리 추가, `listReservations()`의 자동 정리 호출 제거.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 여전히 백그라운드 크론은 아니므로 관리자가 관리자 영역에 진입할 때 정리된다.
- 다음 단계: 배포 후 `/admin/settings`, `/admin/menu` 등으로 들어가도 지난 예약이 정리되는지 확인한다.
## 2026-05-26 - 예약 방문 시간 과거 옵션 제거

- 사용자 요청 요약: 당일 예약인데 현재 시각보다 이전 시간까지 방문 시간 선택창에 노출되어 불필요하고 혼란스럽다고 지적.
- 진행한 문제 또는 요청: 예약 시간 옵션을 현재 시간 이후부터 마감 전까지 10분 단위로만 보여주도록 수정.
- 확인한 원인과 단서: 프론트 예약 폼의 `getTimeOptions()`가 영업 시작 시간부터 마감 시간까지 전체 범위를 그대로 생성하고 있었고, 서버 검증도 영업시간 범위만 확인하고 현재 시각 이전 여부는 막지 않았다.
- 시도한 해결 과정: KST 시간 유틸을 프론트/서버에서 공통 사용하게 정리하고, 현재 시각을 10분 단위로 올림 처리해 가장 빠른 예약 가능 시간으로 사용했다.
- 적용한 변경 사항: 예약 폼 시간 옵션은 `max(오픈시간, 현재시각 올림)`부터 `마감 전`까지만 생성하고, 이미 선택한 시간이 시간이 지나 유효하지 않게 되면 자동 초기화되도록 했다. 서버 검증에도 현재 시각 이전 예약 차단을 추가했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 실제 배포 환경에서는 사용자가 페이지를 오래 열어둔 상태에서 30초 단위 갱신 이후 선택값이 사라질 수 있으나, 과거 시간 예약을 막기 위한 의도된 동작이다.
- 다음 단계: 배포 후 현재 시각 기준으로 예약 시간 선택창에서 지난 시간이 사라지는지 모바일에서 확인한다.
## 2026-05-26 - 관리자 임시 비밀번호 보호 추가

- 사용자 요청 요약: 관리자 페이지에 임시 비밀번호 `1234`를 걸고, 나중에는 DB 기반 비밀번호로 제대로 전환하고 싶다고 요청.
- 진행한 문제 또는 요청: `/admin` 하위 화면 진입 전에 비밀번호 입력 화면을 거치도록 구현.
- 확인한 원인과 단서: 현재 관리자 화면은 별도 인증 없이 직접 접근 가능했고, `/api/admin/settings`는 공개 예약 화면에서도 영업시간/휴무/메뉴 상태를 읽기 때문에 API 전체 차단은 적합하지 않았다.
- 시도한 해결 과정: `/admin/*` 화면만 미들웨어로 보호하고, `/admin/login`은 예외로 열어두었다. 로그인 성공 시 `httpOnly` 쿠키를 발급해 관리자 화면 접근을 허용했다.
- 적용한 변경 사항: 관리자 로그인 페이지, 로그인/로그아웃 서버 액션, 관리자 보호 미들웨어, 관리자 내 로그아웃 버튼, `ADMIN_PASSWORD` 환경변수 예시를 추가했다. 환경변수가 없으면 임시값 `1234`를 사용한다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 현재 방식은 임시 단일 비밀번호 방식이며 사용자별 계정/비밀번호 변경 이력/시도 제한은 없다. 운영 단계에서는 DB 기반 인증과 비밀번호 해시 저장으로 전환해야 한다.
- 다음 단계: 배포 후 `/admin/reservations` 접속 시 `/admin/login`으로 이동하고, `1234` 입력 후 원래 관리자 화면으로 들어가는지 확인한다.
## 2026-05-26 - 상단 관리자 버튼 숨김

- 사용자 요청 요약: PC 상단 네비게이션에 관리자 버튼이 너무 대놓고 보여서 제거하고, 맨 아래의 작은 관리자 링크로만 들어가게 하고 싶다고 요청.
- 진행한 문제 또는 요청: 홈페이지 상단의 관리자 링크를 제거.
- 확인한 원인과 단서: `app/page.tsx` 상단 네비게이션에 `/admin/reservations` 링크가 직접 노출되어 있었고, 모바일 CSS에서는 해당 링크를 숨기는 규칙이 따로 남아 있었다.
- 적용한 변경 사항: 상단 네비게이션의 관리자 링크를 삭제하고, 더 이상 필요 없는 모바일 숨김 CSS를 제거했다. 푸터의 `footer-admin-link`는 유지했다.
- 검증 결과: 빌드/타입 체크 예정.
- 남은 위험 요소: 푸터 관리자 링크는 여전히 접근 가능하며, 직접 `/admin/*` URL 접근은 비밀번호 로그인으로 보호된다.
- 다음 단계: 배포 후 PC 상단에는 메뉴/예약하기만 보이고 하단에서만 관리자 진입이 가능한지 확인한다.
## 2026-05-26 - 실제 위치 주소 반영

- 사용자 요청 요약: 위치를 기존 예시 주소에서 `강원특별자치도 홍천군 홍천읍 홍천로5길 10 1층`으로 변경하고, 이후 위치 관련 정보는 이 주소를 사용해달라고 요청.
- 진행한 문제 또는 요청: 사이트 공통 설정의 주소 값을 실제 주소로 교체.
- 확인한 원인과 단서: 위치 섹션은 `siteConfig.address`를 통해 주소를 표시하고 있어 상수 파일 한 곳만 수정하면 화면 전체에 반영된다.
- 적용한 변경 사항: `lib/constants/site.ts`의 `address` 값을 새 주소로 변경했다.
- 검증 결과: 빌드/타입 체크 예정.
- 남은 위험 요소: 지도 링크나 외부 길찾기 연동은 아직 없으므로, 추후 지도 기능을 추가할 때도 이 주소를 기준으로 사용해야 한다.
- 다음 단계: 배포 후 위치 및 층별 안내 섹션의 주소가 새 주소로 보이는지 확인한다.
## 2026-05-26 - 영업 상태 문구 간소화

- 사용자 요청 요약: 히어로의 영업 상태에서 `오늘 영업시간` 문구가 길어 보여 `영업시간`으로 줄이고 싶다고 요청.
- 적용한 변경 사항: `app/page.tsx`의 영업 상태 상세 문구를 `영업시간 00:00 - 23:50` 형식으로 변경했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 배포 후 히어로 영업 상태 문구가 간결하게 보이는지 확인한다.
## 2026-05-26 - 예약 섹션 안내 문구 제거 및 중앙 정렬

- 사용자 요청 요약: 예약 섹션의 `방문 시간과 원하는 메뉴를 함께 남겨주세요.`, `단체 방문이나 별도 요청은 요청사항에 적어주세요.` 문구를 삭제하고 방문 예약하기 창을 가운데 정렬하고 싶다고 요청.
- 적용한 변경 사항: 홈 예약 섹션의 안내 문구 블록을 제거하고, 예약 폼 카드가 중앙에 오도록 `cta-layout`을 단일 중앙 정렬 구조로 변경했다. 홈 compact 예약 카드 내부의 보조 문구도 숨겼다.
- 검증 결과: 빌드/타입 체크 예정.
- 다음 단계: 배포 후 예약 섹션이 문구 없이 중앙 카드 중심으로 보이는지 확인한다.
## 2026-05-26 - 메뉴판 섹션 중앙 정렬

- 사용자 요청 요약: 메뉴판 섹션도 방문 예약하기 카드처럼 중앙으로 모으고, 왼쪽 사진 영역을 제거하고 싶다고 요청.
- 적용한 변경 사항: 메뉴 섹션의 왼쪽 메뉴판 사진 figure를 제거하고, 메뉴 목록 컨테이너를 중앙 정렬 및 최대 폭 제한 구조로 변경했다.
- 검증 결과: 빌드/타입 체크 예정.
- 다음 단계: 배포 후 메뉴판 섹션이 한쪽으로 치우치지 않고 중앙에 모여 보이는지 확인한다.
## 2026-05-26 - 방문 예약 카드 확대

- 사용자 요청 요약: 방문 예약하기 예약 창을 현재 크기에서 약 1.8배 크게 키우고 싶다고 요청.
- 적용한 변경 사항: 홈 예약 섹션의 예약 폼 카드 최대 폭을 620px에서 1120px로 확대하고, 해당 카드 내부 여백/제목/입력칸 높이를 함께 키워 넓어진 카드에 맞게 조정했다.
- 검증 결과: 빌드/타입 체크 예정.
- 다음 단계: 배포 후 PC 화면에서 예약 카드가 넓게 보이고, 모바일에서는 화면 폭 안에서 자연스럽게 줄어드는지 확인한다.
## 2026-05-26 - 메뉴판 선택과 예약 장바구니 연결

- 사용자 요청 요약: 메뉴판과 예약 폼 양쪽에서 메뉴를 반복 표시하는 정보 중복을 줄이고, 메뉴판의 HOT/ICE/가격 영역을 클릭하면 예약 폼 장바구니에 담기게 하고 싶다고 요청.
- 진행한 문제 또는 요청: 메뉴판 섹션을 선택 진입점으로 만들고, 예약 폼의 `메뉴 고르기` 영역은 선택된 메뉴만 보여주는 `장바구니`로 변경.
- 확인한 원인과 단서: 기존 예약 폼이 전체 메뉴 목록과 수량 버튼을 다시 렌더링해 메뉴판 섹션과 역할이 겹쳤다.
- 적용한 변경 사항: `MenuOrderBoard` 클라이언트 컴포넌트를 추가해 메뉴 가격 버튼 클릭 이벤트를 발행하고, `ReservationForm`이 이 이벤트를 받아 장바구니 수량을 갱신하도록 연결했다. 예약 폼에서는 전체 메뉴 목록을 제거하고 담긴 메뉴/총 잔 수/총액/수량 조절만 표시한다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과. 임시 서버는 정상 시작했으나 현재 프로젝트와 노드 REPL 환경에 Playwright가 없어 자동 브라우저 클릭 검증은 수행하지 못했다.
- 남은 위험 요소: 실제 모바일 터치감과 메뉴 버튼 줄바꿈은 배포 화면에서 한 번 확인해야 한다.
- 다음 단계: 배포 후 메뉴판의 HOT/ICE/가격 버튼을 눌러 예약 장바구니에 항목이 추가되는지 확인한다.
## 2026-05-26 - 메뉴 섹션 제목 및 메뉴 운영 문구 정리

- 사용자 요청 요약: 메뉴 섹션의 `Menu / 메뉴판` 텍스트를 지우고 중앙에 큰 `MENU`만 보이게 하며, 관리자 메뉴 운영 설명을 예약 화면 기준이 아닌 메뉴판 기준으로 바꾸고 싶다고 요청.
- 적용한 변경 사항: 홈 메뉴 섹션 제목을 중앙 정렬 대문자 `MENU`로 변경했다. 관리자 메뉴 운영 페이지의 안내 문구를 `메뉴판에 노출할 메뉴와 품절 상태를 관리합니다.`로 수정하고, 세부 설명도 메뉴판 기준으로 맞췄다.
- 확인한 동작: 메뉴판은 이미 관리자 설정을 사용해 숨김 메뉴를 제외하고, 품절 메뉴는 가격 버튼 대신 `품절`로 표시하도록 연결되어 있다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 배포 후 메뉴 운영에서 숨김/품절을 바꿨을 때 홈 메뉴판에 반영되는지 화면에서 확인한다.
## 2026-05-26 - MENU 제목 크기/색상 조정

- 사용자 요청 요약: 홈 메뉴 섹션의 `MENU` 영어 제목이 너무 커서 절반 크기로 줄이고, 기존 메뉴판 제목에 쓰던 녹색 계열로 바꾸고 싶다고 요청.
- 적용한 변경 사항: `MENU` 제목 색상을 `var(--accent)`로 변경하고, 폰트 크기 clamp 값을 기존 대비 절반 수준으로 축소했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 배포 후 메뉴 섹션 제목이 과하게 튀지 않고 녹색으로 보이는지 확인한다.
## 2026-05-26 - MENU 제목 둥근 폰트 적용

- 사용자 요청 요약: 사용자가 포토샵으로 만든 참고 이미지처럼 `MENU`를 더 둥글고 부드러운 영어 폰트로 바꾸고, 인터넷에서 폰트를 받아 저작권 문제 여부도 확인해달라고 요청.
- 확인한 원인과 단서: 기존 `MENU`는 기본 산세리프 계열이라 각진 인상이 강했다. `M PLUS Rounded 1c`는 Google Fonts/Fontsource에서 제공되고 `OFL-1.1`로 배포되어 웹사이트 번들/상업적 사용에 적합하다.
- 적용한 변경 사항: `M PLUS Rounded 1c` Black 900 폰트 파일을 `public/fonts`에 로컬 저장하고, OFL 라이선스 파일을 함께 추가했다. `MENU` 제목에 해당 폰트를 적용하고 녹색/중앙 정렬 분위기는 유지했다.
- 검증 결과: 빌드/타입 체크 예정.
- 남은 위험 요소: 폰트 파일 용량이 약 3.5MB로 작지는 않다. 현재는 `MENU` 한 곳만 사용하므로 추후 성능이 민감하면 라틴 서브셋 파일로 줄이는 방법을 검토할 수 있다.
- 다음 단계: 배포 후 참고 이미지처럼 둥근 MENU 느낌이 나는지 화면에서 확인한다.
## 2026-05-26 - 히어로 제목 줄 간격 조정

- 사용자 요청 요약: 맨 위 히어로 문구 `오늘은 조금 천천히 / 꽃신에서 쉬어가세요`의 줄바꿈 사이 간격이 너무 좁아 더 넓히고 싶다고 요청.
- 적용한 변경 사항: `.hero-copy h1`의 `line-height`를 `1.02`에서 `1.16`으로 조정해 두 줄 사이 여백을 늘렸다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 배포 후 모바일/PC에서 히어로 제목 줄 간격이 답답하지 않은지 확인한다.
## 2026-05-26 - 장바구니 빈 상태 안내 문구 수정

- 사용자 요청 요약: 예약 장바구니 빈 상태 문구에서 `HOT, ICE 또는 가격 버튼` 대신 `HOT, ICE 버튼`으로 간단히 바꾸고 싶다고 요청.
- 적용한 변경 사항: `위 메뉴판에서 HOT, ICE 버튼을 눌러 담아주세요.`로 문구를 변경했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 배포 후 방문 예약하기 장바구니 빈 상태 문구를 확인한다.
## 2026-05-26 - 디자인 스킬 기준 전체 랜딩 디자인 리팩토링

- 사용자 요청 요약: 기능은 유지하되, 사용자가 설치/설정한 디자인 스킬과 디자인 링크 기준을 참고해 페이지 디자인 자체는 크게 바꿔도 된다고 요청.
- 진행한 문제 또는 요청: `design-taste-frontend`, `web-design-guidelines`, `ui-ux-pro-max` 관점과 외부 휴리스틱 기준을 확인하고, 홍천 꽃신 사이트의 랜딩 비주얼 밀도와 예약 사용성을 개선.
- 확인한 원인과 단서: 기존 화면은 기능 흐름은 유지되고 있었지만 섹션 간 연결감, 사진 존재감, 메뉴/예약 영역의 시각적 밀도, 모바일 히어로 문구 안정성이 부족했다.
- 시도한 해결 과정: 외부 휴리스틱 페이지와 Vercel Web Interface Guidelines 원문을 확인한 뒤, 기능 컴포넌트 대신 `app/globals.css` 중심으로 디자인만 교체했다. 빌드 후 Chrome headless로 데스크톱/모바일 히어로를 확인하며 모바일 제목 잘림을 조정했다.
- 적용한 변경 사항: 따뜻한 종이톤/세이지/우드 계열 토큰 강화, 플로팅 내비게이션, 큰 이미지형 히어로, 사진 그리드/방문 안내/어바웃/메뉴판/예약 카드의 배경과 여백 재구성, 메뉴 HOT/ICE 버튼과 예약 장바구니의 터치/호버/포커스 가시성 개선, `prefers-reduced-motion` 대응 추가.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과. 검사용 3107 포트로 `next start`를 잠깐 실행해 Chrome headless 모바일/데스크톱 스크린샷을 확인했고, 확인 후 서버 프로세스를 종료했다.
- 남은 위험 요소: 앵커 URL을 headless Chrome에서 직접 열었을 때 특정 섹션 스크린샷이 정확히 해당 위치에서 시작하지 않는 현상이 있어, 실제 브라우저에서 메뉴/예약 버튼 스크롤 체감은 한 번 더 눈으로 검수하는 것이 좋다.
- 다음 단계: 배포 후 실제 모바일에서 히어로, 메뉴 버튼 클릭 → 예약 장바구니 추가, 예약 카드 입력 흐름을 확인한다.

## 2026-05-26 - 히어로 높이 축소

- 사용자 요청 요약: 상단 요소가 사라진 뒤 첫 히어로 영역이 너무 커 보여 높이를 줄이고 싶다고 요청.
- 진행한 문제 또는 요청: 기능은 유지하고 `app/globals.css`에서 히어로 높이와 상하 패딩만 조정.
- 확인한 원인과 단서: 이전 리디자인에서 사진 중심 분위기를 살리려고 히어로를 100vh 이상으로 크게 잡아, 다음 위치/층별 안내 섹션 진입이 늦어졌다.
- 적용한 변경 사항: 기본 히어로를 104vh에서 88vh로, 모바일은 100svh에서 86svh로, 데스크톱 와이드는 108vh에서 90vh로 줄였다. 하단 패딩도 함께 줄여 첫 화면 아래에 다음 섹션이 더 빨리 보이게 했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과. 검사용 3107 포트에서 모바일 스크린샷을 확인했고 서버는 종료했다.
- 다음 단계: 실제 휴대폰에서 첫 화면이 너무 짧거나 길지 않은지 체감 확인.

## 2026-05-26 - 레퍼런스 기반 과감한 페이지 구조 재설계

- 사용자 요청 요약: 기존 플롯과 카드 섹션을 유지한 보수적 수정이 아니라, 백업이 있으니 참고 디자인 기준에 맞춰 사이트 배치 자체를 과감하게 바꿔도 된다고 요청.
- 진행한 문제 또는 요청: 기존 `히어로 -> 위치 -> 공간 -> 어바웃 -> 메뉴 -> 예약`의 분리형 흐름을 더 편집적이고 눈에 들어오는 랜딩 구조로 재설계.
- 확인한 원인과 단서: 이전 수정은 색/표면/일부 여백 개선에 가까워, 사용자가 원하는 레퍼런스식 비대칭 구성과 강한 첫인상에는 부족했다.
- 적용한 변경 사항: `app/page.tsx`에서 히어로를 왼쪽 대형 문구와 오른쪽 방문 정보 패널로 재구성하고, 위치/층별 안내를 풀블리드 2분할 이미지 섹션으로 배치했다. About은 비대칭 매거진형 섹션으로, 공간 사진은 가로 흐름의 콜라주로, 메뉴와 예약은 같은 주문 워크스페이스 안에 결합했다. `app/globals.css`에는 새 편집형 레이아웃, 모바일 단일 컬럼/가로 스크롤 갤러리, 메뉴+예약 패널 스타일을 추가했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과. Chrome headless로 데스크톱/모바일 첫 화면을 확인했고, 모바일 제목 잘림을 추가로 조정했다.
- 남은 위험 요소: 메뉴+예약 섹션은 실제 브라우저에서 앵커 스크롤 위치와 장바구니 흐름을 한 번 더 직접 확인하는 것이 좋다.
- 다음 단계: 사용자가 화면을 보고 방향이 맞으면 세부 이미지 크롭, 메뉴/예약 패널 밀도, 모바일 하단 CTA 위치를 추가 조정한다.

## 2026-05-26 - `.next/server/pages/_document.js` 누락 오류 복구

- 사용자 문제 제시: 로컬 실행 중 `ENOENT: no such file or directory, open '.next\\server\\pages\\_document.js'` 런타임 오류가 발생.
- 확인/분석: 3000번 포트에 Node 서버가 살아 있었고, `.next/server/app/page.js`는 존재했지만 `.next/server/pages/_document.js`가 사라져 빌드 산출물이 반쯤 깨진 상태였다.
- 해결 시도: 3000번 Listen 프로세스를 종료하고, 워크스페이스 내부의 `.next` 폴더와 `tsconfig.tsbuildinfo`만 삭제한 뒤 `npm run build`로 산출물을 재생성했다.
- 결과: `.next/server/pages/_document.js`와 `.next/server/app/page.js`가 모두 다시 생성되었고, `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 사용자는 프로젝트 루트에서 `npm run dev`로 서버를 다시 켜면 된다.

## 2026-05-26 - 데스크톱 히어로 세로 범위 1.6배 확대

- 사용자 요청 요약: 현재 히어로 세로 범위를 1이라고 두고 1.6배만큼 먼저 늘려달라고 요청. 모바일은 괜찮으므로 데스크톱 기준으로 조정.
- 적용한 변경 사항: `app/globals.css`의 `.landing > .hero` 데스크톱 기본값을 `min(760px, 82vh)`에서 `min(1216px, 131vh)`로, 패딩을 `6.4rem 1rem 3.2rem`에서 `10.24rem 1rem 5.12rem`으로 1.6배 확대했다. 모바일 오버라이드는 유지했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 실제 데스크톱 화면에서 히어로 체감 높이가 원하는 값에 가까운지 확인한다.

## 2026-05-26 - 데스크톱 중심 위치/About/예약 영역 밀도 조정

- 사용자 요청 요약: 위치/층별 안내 사진 영역이 너무 커서 와이드 기준 위아래를 줄이고, About은 제목을 위로 올린 뒤 사진과 문구를 아래에 두는 이전에 가까운 구조가 좋겠다고 요청. 초록색 영문 라벨, 예약 제목은 제거하고, 장바구니 안내 문구와 데스크톱 예약 패널 답답함도 수정 요청.
- 적용한 변경 사항: 위치/층별 안내 섹션의 데스크톱 높이를 520px에서 380px로 줄이고 제목/주소 텍스트도 작게 조정했다. About은 `story-title` 아래 `story-body`를 추가해 사진+문구 2열 구조로 재배치했다. Location/Floor/About/Space/Menu & Reservation 라벨을 마크업에서 제거했고, 예약폼 제목은 빈 값으로 전달하며 CSS에서 숨겼다. 예약 워크스페이스는 1360px로 넓히고 예약 패널 컬럼을 키웠다. 장바구니 빈 문구를 `메뉴판에서 HOT 또는 ICE 버튼을 눌러 담아주세요.`로 수정했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과. 타입 체크는 빌드와 병렬 실행 시 `.next/types` 타이밍 충돌이 있어 단독 재실행했다.
- 다음 단계: 데스크톱에서 위치/층별, About, 예약 패널 밀도가 원하는 정도인지 확인한다.

## 2026-05-26 - 갤러리/주문 섹션 간격과 데스크톱 내비게이션 조정

- 사용자 요청 요약: `꽃신을 소개합니다` 제목과 사진 사이 여백을 더 넓히고, `메뉴를 담고, 방문 시간을 남겨주세요` 문구는 지워 메뉴가 위로 붙게 하며, 컴퓨터에서는 상단 메뉴/예약 버튼 중 메뉴 버튼을 없애달라고 요청.
- 적용한 변경 사항: `.runway-head` 하단 여백을 기존의 약 2배로 늘렸다. 주문 섹션의 `order-head` 마크업을 제거하고 `.order-section` 상단 패딩을 줄여 메뉴 패널이 위로 올라오게 했다. `min-width: 768px` 이상에서 `.top-nav-actions a:first-child`를 숨겨 데스크톱에는 예약 버튼만 보이게 했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 데스크톱에서 갤러리와 주문 섹션의 간격이 적절한지 확인한다.

## 2026-05-26 - 관리자 페이지 CSS 404 복구

- 사용자 문제 제시: `/admin/reservations`에 들어갔을 때 스타일이 전혀 적용되지 않은 기본 HTML 화면이 보이고, F5를 눌러도 그대로라고 보고.
- 확인/분석: 관리자 HTML은 `/_next/static/css/app/layout.css`를 참조했지만 현재 3000번 dev 서버에서 해당 CSS가 404였다. `.next` 산출물과 실행 중인 dev 서버가 꼬인 상태로 판단.
- 해결 시도: 3000번 Listen 프로세스를 종료하고 `.next`와 `tsconfig.tsbuildinfo`를 삭제한 뒤 `npm run build`로 산출물을 재생성했다. 이후 임시 dev 서버를 3110 포트에 띄워 관리자 페이지 CSS 링크가 200으로 응답하는지 확인했다.
- 결과: `npm run build`, `npx tsc --noEmit` 통과. 임시 dev 서버에서 `/_next/static/css/app/layout.css`가 200으로 정상 응답했다. 임시 서버는 종료했다.
- 다음 단계: 사용자는 프로젝트 루트에서 `npm run dev`로 3000번 서버를 다시 켜고 관리자 페이지를 새로 열면 된다.
## 2026-05-26 - 문의전화 버튼과 사장님 번호 설정 추가

- 사용자 요청 요약: 상단에 `문의전화` 버튼을 추가하고, 컴퓨터에서는 사장님 번호가 팝업으로 보이며 휴대폰에서는 전화 앱에 번호가 입력된 상태로 연결되게 해달라고 요청. 기본 번호는 `01043303764`로 설정하고, 관리자 알림 설정에서 사장님 번호를 관리할 수 있게 해달라고 요청.
- 진행한 문제 또는 요청: 홈페이지 상단 내비게이션에 문의전화 액션을 추가하고, 관리자 설정 데이터에 전화번호 필드를 안정적으로 저장/조회하도록 구성.
- 확인한 원인과 단서: 기존 관리자 알림 설정 구조에 `phone` 필드는 있었지만 빈 값 fallback과 별도 편집 UI가 부족했고, 홈페이지 상단에서 해당 값을 사용하는 버튼이 없었다.
- 시도한 해결 과정: 기본 전화번호 상수를 추가하고 설정 파싱/저장 시 빈 값이면 기본 번호를 사용하도록 보강했다. 데스크톱과 모바일 동작을 분기하는 클라이언트 문의전화 버튼을 만들고, 관리자 알림 설정 페이지에 전화번호 저장 폼을 추가했다.
- 적용한 변경 사항: `lib/admin/settings.ts`, `app/page.tsx`, `app/admin/settings/page.tsx`, `components/contact/phone-inquiry-button.tsx`, `components/admin/owner-phone-form.tsx` 수정/추가.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 데스크톱 팝업은 브라우저 기본 alert를 사용하므로 디자인 커스텀 팝업이 필요하면 추후 모달로 바꿀 수 있다.
- 다음 단계: 실제 모바일에서 `문의전화` 버튼을 눌렀을 때 전화 앱이 열리고 번호가 정확히 입력되는지 확인한다.

### 추가 수정
- 사용자 요청 요약: 버튼 문구를 `문의전화`가 아니라 `전화문의`로 바꾸고 싶다고 요청.
- 적용한 변경 사항: 홈페이지 상단 버튼, 데스크톱 팝업 제목, 관리자 번호 설정 안내/라벨/저장 메시지의 표시 문구를 `전화문의`로 통일했다.

## 2026-05-26 - About 제목 크기와 하단 간격 조정

- 사용자 요청 요약: `홍천 신장대리에 피어난 작은 쉼터` 제목 크기를 줄이고, 제목 아래 사진/글 영역과 너무 붙어 보이지 않게 패딩/간격을 넣어달라고 요청.
- 적용한 변경 사항: `app/globals.css`에서 About 섹션 제목 크기를 낮추고, `.story-layout`의 간격을 키워 제목과 본문 이미지/글 묶음 사이 여백을 늘렸다.
- 검증 결과: 파일 반영 여부는 확인했지만, 사용자가 중간에 멈춘 직후라 빌드 검증은 아직 다시 돌리지 않았다.

## 2026-05-26 - 세로 모니터 메뉴/예약 겹침 수정

- 사용자 문제 제시: 컴퓨터 세로 모니터에서 메뉴판과 예약 폼이 좌우 2단으로 유지되면서 메뉴 가격 버튼이 예약 폼 영역 아래로 겹쳐 보이는 화면을 공유.
- 확인한 원인과 단서: `900px` 초과부터 주문 영역이 데스크톱 2단 그리드로 고정되고, 메뉴판 내부도 2열이라 900~1180px 사이의 좁은 데스크톱 폭에서 가로 공간이 부족했다.
- 적용한 변경 사항: `901px~1180px` 구간에서는 주문 영역을 1열로 전환하고 예약 폼 sticky를 해제해 메뉴판 아래로 자연스럽게 내려가도록 조정했다.
- 검증 결과: CSS 변경 반영 여부는 확인했으며, 긴 빌드 검증은 아직 실행하지 않았다.

## 2026-05-26 - 모바일 히어로/갤러리 제목/시간 선택 조정

- 사용자 문제 제시: 모바일 시간 선택의 `방문 시간을 선택해주세요` 문구가 잘리고, 모바일에서 `꽃신을 소개합니다`가 어색하게 줄바꿈되며, 모바일 히어로 영역이 너무 작다고 요청.
- 적용한 변경 사항: 모바일에서 시간 선택 select 폰트를 줄이고 내부 여백을 조정했다. `꽃신을 소개합니다`는 모바일에서 `꽃신을 / 소개합니다`로 줄바꿈되도록 전용 줄바꿈을 추가했다. 모바일 히어로 높이와 상하 패딩을 기존 대비 약 1.3배로 늘렸다.
- 검증 결과: 변경 파일과 diff를 확인했으며, 긴 빌드 검증은 아직 실행하지 않았다.

## 2026-05-26 - 24시간 운영 설정 반영 지연/영업 종료 표시 수정

- 사용자 문제 제시: 관리자 모드에서 24시간 운영 기능을 켰는데도 사이트에서 영업이 종료되었다고 표시된다고 보고.
- 확인한 원인과 단서: 홈페이지가 빌드 결과에서 정적 페이지로 잡히면 관리자 설정과 현재 영업 상태가 배포 시점 값으로 굳을 수 있었다. 또한 관리자 설정 조회에 30초 서버 캐시가 있어 저장 직후 예전 설정을 볼 가능성이 있었다.
- 적용한 변경 사항: 홈페이지를 `force-dynamic`/`revalidate = 0`로 전환하고, 관리자 설정 API와 예약 폼 설정 조회를 `no-store`로 보강했다. 설정 서버 캐시를 끄고, `영업시간 24시간` 체크박스는 저장 버튼 없이 즉시 저장되도록 바꿨다.
- 검증 결과: `npx tsc --noEmit` 통과. `npm run build`는 120초 제한으로 타임아웃되어 완료 판정은 못 했으며, 남은 Node 프로세스가 사용자 로컬 서버일 수 있어 종료하지 않았다.

## 2026-05-26 - 모바일 히어로 높이 재조정

- 사용자 문제 제시: 모바일 히어로에서 `꽃신에서 쉬어가세요` 문구가 하단 고정 `방문 예약하기` 버튼에 가려져 보인다고 보고.
- 적용한 변경 사항: 모바일 히어로를 기존 대비 1.3배로 키웠던 값을 약 1.2배 수준으로 낮춰 하단 CTA와의 겹침을 줄였다.

### 추가 수정
- 사용자 요청 요약: 1.2배는 조금 작아서 1.25배 수준으로 다시 키워달라고 요청.
- 적용한 변경 사항: 모바일 히어로 높이와 상하 패딩을 1.25배 수준으로 재조정했다.

## 2026-05-26 - 친구 피드백 기반 예약 인지와 메뉴 선택 UX 개선

- 사용자 문제 제시: 첫 진입 시 카페 예약 사이트인지 바로 모르겠다는 피드백과, 메뉴 버튼을 여러 번 누르다가 장바구니에 같은 메뉴가 과도하게 담기는 문제가 있었다고 공유.
- 확인한 원인과 단서: 히어로 문구가 감성 중심이라 예약 목적이 약했고, 메뉴 버튼 클릭이 장바구니 추가 동작임에도 버튼 자체에 선택 상태/담김 수량 피드백이 부족했다.
- 적용한 변경 사항: 히어로에 `전통차와 디저트를 미리 담고, 오늘 방문 시간을 예약하는 카페 예약 사이트입니다.` 안내 문구를 추가했다. 메뉴 상단에 HOT/ICE 버튼 안내를 추가하고, 메뉴 버튼이 담긴 상태를 초록색 `담김 1`로 표시하도록 했다. 같은 메뉴 버튼을 반복 클릭해도 수량이 계속 증가하지 않고, 수량 변경은 장바구니의 +/-에서 하도록 흐름을 바꿨다.
- 검증 결과: 타입체크 예정.

## 2026-05-27 - 주문 접근성 우선 구조와 모바일 메뉴 장르 탭 적용

- 사용자 요청 요약: 히어로의 설명 문구는 제거하고, 상단 브랜드를 `CAFE 홍천 꽃신`으로 변경. 사이트 흐름은 히어로 다음 바로 메뉴/예약이 오고, 이후 위치/층별 안내, 사진 소개, About 순서로 끝나게 변경 요청. 모바일에서는 전체 메뉴를 길게 스크롤하지 않고 장르 칸을 가로 스와이프해 선택한 장르 메뉴만 보이게 해달라고 요청.
- 적용한 변경 사항: 히어로 예약 설명 문구를 제거하고 브랜드명을 바꿨다. 주문 섹션을 히어로 바로 아래로 이동하고, 위치/층별 안내와 갤러리, About 순서를 재배치했다. 모바일 메뉴판에는 장르 선택 탭을 추가해 선택한 장르의 메뉴만 렌더링되도록 했다.
- 남은 확인 사항: 실제 모바일에서 장르 탭의 스와이프 감각과 메뉴 선택 후 장바구니 위치 인지가 충분한지 확인 필요.

## 2026-05-27 - 메뉴 선택 피드백 토스트 방식으로 변경

- 사용자 요청 요약: 메뉴 버튼이 `HOT 담김 1`처럼 바뀌는 표기가 별로라 이전 가격 표기로 되돌리고, 대신 메뉴 선택 시 `장바구니에 상품명이 추가되었습니다!` 같은 팝업형 피드백을 띄워달라고 요청.
- 적용한 변경 사항: 메뉴 버튼 표기를 `HOT 가격`, `ICE 가격` 형태로 복구했다. 메뉴 클릭 시 메뉴판 상단에 토스트 메시지가 뜨도록 추가했고, 이미 담긴 메뉴를 다시 누르면 장바구니에 이미 담겼다는 안내를 띄우도록 했다.
- 검증 결과: `npx tsc --noEmit` 통과.

## 2026-05-27 - 페이지 이동 체감 속도 최적화

- 사용자 문제 제시: 페이지 간 이동 시 기능과 디자인은 건드리지 말고 버벅임과 지연을 줄일 수 있는지 문의.
- 확인한 원인과 단서: 24시간 운영 버그 수정 때 관리자 설정 캐시를 완전히 꺼둔 상태라 페이지 이동마다 설정 DB 조회가 잦아질 수 있었다. 관리자 내비게이션 링크는 기본 prefetch에 맡겨져 있었고, 하단 이미지 섹션은 초기 렌더링 비용이 큰 편이었다.
- 적용한 변경 사항: 관리자 설정 서버 캐시를 5초로 복구하되 저장 시 즉시 최신 값으로 갱신되게 유지했다. 관리자 내비게이션과 푸터 관리자 링크에 `prefetch`를 명시했다. 위치/갤러리/About 섹션에 `content-visibility: auto`와 intrinsic size를 추가해 화면 밖 섹션 렌더링 부담을 줄였다.
- 검증 결과: `npx tsc --noEmit` 통과.

### 추가 수정
- 사용자 피드백: 이미 담긴 메뉴를 다시 눌렀을 때 차단하지 말고, 여러 번 누르면 수량이 늘어나야 한다고 지적.
- 적용한 변경 사항: 메뉴 버튼을 누를 때마다 장바구니 수량이 증가하도록 되돌리고, 토스트는 매번 `장바구니에 상품명이 추가되었습니다!` 메시지만 뜨도록 수정했다.
- 검증 결과: `npx tsc --noEmit` 통과.

### 추가 수정 2
- 사용자 문제 제시: 레몬에이드를 두 번 눌렀는데 장바구니 수량이 2개로 증가하지 않는다고 보고.
- 적용한 변경 사항: 메뉴 추가 이벤트를 받는 예약 폼에서 직접 `selectedMenus[label] + 1`을 처리하도록 단순화했다. 같은 메뉴를 연속으로 눌러도 토스트가 매번 새로 표시되도록 토스트 상태에 id를 추가했다.
- 검증 결과: `npx tsc --noEmit` 통과.

### 추가 수정 3
- 사용자 요청 요약: 메뉴 추가 토스트의 초록색이 메뉴판과 비슷해 별로라 파란색으로 변경하고, 모바일 메뉴 영역 오른쪽에 장바구니로 바로 내려가는 버튼을 추가해달라고 요청.
- 적용한 변경 사항: 토스트 배경을 파란색 계열로 변경했다. 모바일 메뉴 제목 영역에 `장바구니` 바로가기 버튼을 추가하고, 예약 폼 장바구니 섹션에 `reservation-cart` 앵커를 부여했다.
- 검증 결과: `npx tsc --noEmit` 통과.

## 2026-05-27 - 히어로 영업 상태 배지 문구 조정

- 사용자 요청 요약: 히어로의 `현재 영업 중 / 영업시간` 배지를 `CAFE 현재 영업 중 / 영업시간 ...` 형태로 보이게 요청.
- 적용한 변경 사항: 영업 상태 라벨 앞에 `CAFE`를 붙여 카페 상태 정보로 더 명확히 보이도록 수정했다.
