# RETROSPECTIVE

## 2026-05-25 - 관리자 메뉴 정리

- 사용자 요청 요약: 관리자 쪽의 `홈페이지 설정` 항목은 더 이상 필요 없어 보이므로 제거 요청.
- 진행한 문제: 관리자 내 설정 페이지 구조에서 홈페이지 설정 진입점과 관련 UI 잔여물을 정리.
- 확인한 원인과 단서: `AdminNav`에는 이미 홈페이지 설정 메뉴가 제거되어 있었고, 삭제된 라우트의 빈 폴더와 미사용 CSS가 남아 있었다.
- 시도한 해결 과정: `homepage`, `홈페이지`, `admin-photo-preview` 관련 참조를 검색하고 남은 CSS와 빈 라우트 폴더를 제거했다.
- 적용한 변경 사항: `app/admin/homepage` 제거, 관리자 사진 미리보기 CSS 제거, 홈페이지 설정 메뉴/모드 참조 없음 확인.
- 검증 결과: `npx tsc --noEmit` 통과. 삭제된 라우트 때문에 남아 있던 `.next/types/app/admin/homepage` 캐시는 제거 후 재검증했다.
- 남은 위험 요소: `.next` 캐시는 개발 서버 실행 상태에 따라 다시 꼬일 수 있으므로, 라우트 삭제 후 이상하면 개발 서버 재시작이 필요하다.
- 다음 단계: 실제 브라우저에서 관리자 페이지 탭 이동이 의도대로 보이는지 확인하면 된다.

## 2026-05-25 - 예약 기록/메뉴 운영 UX 수정

- 사용자 요청 요약: `전체 기록`은 진행 중과 처리 기록이 한 화면 안에서 나뉘어 보이는 이전 방식이 더 좋고, 메뉴 운영의 품절/숨김 체크박스는 더 크게 보이며 저장 버튼 없이 즉시 적용되길 원함.
- 진행한 문제: 예약 목록의 전체 기록 표시 방식과 메뉴 운영 저장 흐름을 조정.
- 확인한 원인과 단서: 전체 기록 탭이 모든 상태를 하나의 테이블에 섞어 보여 상태 흐름이 덜 명확했고, 메뉴 운영은 체크 후 별도 저장 버튼을 눌러야 해서 조작감이 번거로웠다.
- 시도한 해결 과정: 예약 탭 컴포넌트에서 전체 기록일 때만 `진행 중`과 `처리 기록` 테이블을 분리하고, 메뉴 운영 체크 변경 시 바로 설정 API에 PATCH 하도록 변경했다.
- 적용한 변경 사항: 메뉴 운영의 품절/숨김 체크박스 크기와 터치 영역 확대, 메뉴 운영 페이지 저장 버튼 숨김, 체크 즉시 저장 메시지 표시.
- 검증 결과: `npx tsc --noEmit` 통과.
- 남은 위험 요소: 체크 즉시 저장은 네트워크가 느릴 때 잠깐 비활성화되므로 실제 브라우저에서 클릭 반응감을 확인할 필요가 있다.
- 다음 단계: 로컬 서버 재시작 후 `/admin/reservations`, `/admin/menu`에서 화면 배치와 즉시 저장 동작을 확인한다.

## 2026-05-25 - 예약 완료 문구/번호 표시 개선

- 사용자 요청 요약: 예약 완료 후 UUID가 그대로 보이지 않고 `001` 같은 짧은 예약번호로 보이길 원했고, 완료 문구를 더 부드럽게 변경 요청.
- 진행한 문제: 예약 API 응답에 사용자용 예약번호를 추가하고, 예약 완료 카드의 문구와 줄바꿈 표시를 수정.
- 확인한 원인과 단서: 내부 예약 ID는 UUID로 관리되고 있었고, 프론트 완료 화면에서 이를 그대로 출력하고 있었다.
- 시도한 해결 과정: 저장 구조는 유지하고, 예약 생성 후 같은 날짜의 생성 순서를 계산해 `001` 형식으로 내려주도록 했다.
- 적용한 변경 사항: `reservationNumber` API 응답 추가, 완료 화면 예약번호 표시 변경, 문구를 `예약 요청이 접수되었습니다 : )`로 변경.
- 검증 결과: `npx tsc --noEmit` 통과.
- 남은 위험 요소: 동일 시각 동시 예약이 극단적으로 겹치면 표시 번호 계산이 완전한 DB 시퀀스만큼 엄격하지는 않다.
- 다음 단계: 브라우저에서 새 예약을 넣어 완료 카드에 `001` 형식 번호와 줄바꿈 문구가 보이는지 확인한다.

## 2026-05-25 - PWA/Web Push 알림 테스트 구조 추가

- 사용자 요청 요약: 문자 API 없이 사이트/PWA만으로 휴대폰 예약 알림을 실험할 수 있는지 확인하고, 가능한 부분은 끝까지 구현 요청.
- 진행한 문제: 관리자 휴대폰에서 웹 푸시 구독을 등록하고 새 예약 생성 시 알림을 보내는 기본 구조를 추가.
- 확인한 원인과 단서: 배포 주소가 HTTPS라 PWA/Web Push의 필수 조건은 충족되어 있었고, 문자 API 없이도 VAPID 기반 웹 푸시로 실험 가능했다.
- 시도한 해결 과정: `web-push` 패키지 설치, VAPID 키 생성, 로컬 `.env`에 키 저장, manifest/service worker/API/provider/관리자 버튼을 추가했다.
- 적용한 변경 사항: PWA manifest, `/sw.js`, `/api/push/public-key`, `/api/push/subscriptions`, push subscription 저장소, web-push automation provider, 관리자 알림 설정의 `이 기기에서 알림 받기` UI 추가.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과, 로컬 프로덕션 서버에서 manifest/public-key/service-worker 200 응답 확인.
- 남은 위험 요소: Vercel CLI 로그인이 현재 환경에서 확인되지 않아 배포 환경변수 등록은 아직 직접 완료하지 못했다.
- 다음 단계: Vercel 프로젝트 환경변수에 `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`를 등록하고 재배포한 뒤 휴대폰에서 알림 권한을 테스트한다.

## 2026-05-25 - 관리자 모바일 예약 목록/알림 해제 개선

- 사용자 요청 요약: 모바일에서 예약 목록 오른쪽이 잘려 보이고 관리자 버튼 배치도 모바일에 맞지 않으며, 웹푸시 알림 해제 버튼도 필요함.
- 진행한 문제: 데스크톱 표 중심의 예약 목록을 모바일 카드형으로 병행 표시하고, 관리자 헤더/탭을 모바일 그리드로 재배치.
- 확인한 원인과 단서: 기존 `.admin-table`은 `min-width: 1060px` 표라 모바일에서 가로 스크롤 또는 잘림이 발생했고, 알림 설정 UI에는 구독 등록만 있고 삭제 흐름이 없었다.
- 시도한 해결 과정: 520px 이하에서는 표를 숨기고 카드 리스트를 표시하도록 변경, `/api/push/subscriptions`에 DELETE 추가, 클라이언트에서 `unsubscribe()`와 서버 구독 삭제를 함께 실행하도록 수정.
- 적용한 변경 사항: 예약 카드형 모바일 UI, 관리자 헤더/탭 모바일 레이아웃, `이 기기 알림 해제` 버튼, 알림 해제 API.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과, 로컬 프로덕션 서버에서 `/admin/reservations` 200 응답 확인.
- 남은 위험 요소: Playwright가 현재 런타임에 없어 실제 모바일 스크린샷 자동 검수는 못 했으므로 배포 후 휴대폰에서 여백과 버튼 크기 확인 필요.
- 다음 단계: GitHub 푸시 후 Vercel 배포 완료 시 휴대폰에서 `/admin/reservations`와 `/admin/settings`를 확인한다.

## 2026-05-26 - 예약 날짜 검증 KST 기준 수정

- 사용자 요청 요약: 2026-05-26으로 정상 표시되는데도 예약 제출 시 `예약 날짜는 오늘만 선택할 수 있습니다.` 오류가 발생한다고 제보.
- 진행한 문제: 서버 예약 검증의 오늘 날짜/현재 시간 계산 기준을 확인하고 한국 시간 기준으로 수정.
- 확인한 원인과 단서: Vercel 서버는 UTC 기준으로 동작할 수 있는데, 기존 검증은 `new Date().getFullYear()` 등 서버 로컬 시간 기준으로 오늘을 만들고 있었다. 한국 시간 26일이어도 서버는 25일로 판단할 수 있었다.
- 시도한 해결 과정: `Asia/Seoul` 기준 날짜/시간 유틸을 추가하고 예약 검증 및 홈페이지 영업 상태 표시에서 해당 유틸을 사용하도록 변경했다.
- 적용한 변경 사항: `lib/datetime/korea.ts` 추가, 예약 검증의 today/current time/weekend 계산을 KST 기준으로 변경, 홈페이지 영업 상태도 KST 기준으로 변경.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과, KST 유틸이 `2026-05-26`을 반환하는 것 확인.
- 남은 위험 요소: 클라이언트 휴대폰 시간이 한국 시간이 아닌 경우 화면 표시와 서버 판단이 다를 수 있으나, 한국 카페 예약 서비스 기준으로 서버 KST 판단이 맞다.
- 다음 단계: GitHub 푸시 후 Vercel 배포 완료 시 다시 예약 제출을 확인한다.

## 2026-05-26 - 문자 알림 UI 제거

- 사용자 요청 요약: 웹푸시 알림이 동작하므로 문자 알림 기능의 뿌리는 남기되 관리자 인터페이스에서는 문자 수신 관련 항목을 제거 요청.
- 진행한 문제: 알림 설정 화면에 남아 있던 휴대폰 수신, 문자/SMS 방식 선택, 테스트 수신 문구 UI를 정리.
- 확인한 원인과 단서: `AdminSettingsForm`의 notification 모드가 휴대폰 번호와 문자/SMS 선택지를 렌더링하고 있었고, 알림 설정 페이지에서 해당 폼을 함께 보여주고 있었다.
- 시도한 해결 과정: 알림 설정 페이지를 웹푸시 설정 전용 화면으로 단순화하고, 관리자 설정 폼에서는 영업/휴무와 메뉴 운영 모드만 남겼다.
- 적용한 변경 사항: `/admin/settings`에서 문자 관련 폼 제거, `AdminSettingsForm`의 notification UI 분기 제거, 내부 `NotificationMode` 타입과 저장 구조는 유지.
- 검증 결과: `npm run build` 통과.
- 남은 위험 요소: 내부 자동화 provider 확장용 SMS 타입은 남아 있으므로, 향후 문자 기능을 다시 쓰려면 별도 UI를 새로 붙이면 된다.
- 다음 단계: 배포 후 `/admin/settings`에서 웹푸시 알림 설정만 보이는지 확인한다.

## 2026-05-26 - 지난 예약 자동 처리 완료

- 사용자 요청 요약: 예약 시간이 지나면 자동으로 `처리 완료` 상태로 바뀌게 할 수 있는지 문의.
- 진행한 문제: 별도 스케줄러 없이 관리자 목록/예약 API 조회 시 지난 진행 중 예약을 자동 정리하는 방식으로 구현.
- 확인한 원인과 단서: 현재 예약 목록은 조회 시 DB에서 직접 불러오므로, 조회 직전에 `pending`/`confirmed` 중 날짜와 시간이 지난 예약을 `completed`로 업데이트하면 운영상 자동 처리처럼 동작한다.
- 시도한 해결 과정: 한국 시간 기준 오늘 날짜와 현재 시간을 사용해 `date < today` 또는 `date = today AND time < currentTime`인 진행 중 예약을 일괄 업데이트.
- 적용한 변경 사항: `completeExpiredReservations()` 추가, `listReservations()` 시작 시 자동 호출.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 완전한 백그라운드 작업이 아니라 예약 목록/API가 조회될 때 정리된다. 아무도 관리자를 열지 않으면 그 순간까지 상태 변경은 지연될 수 있다.
- 다음 단계: 배포 후 예약 시간이 지난 항목이 관리자 목록 새로고침 시 처리 기록으로 내려가는지 확인한다.

## 2026-05-26 - 관리자 전체 진입 시 지난 예약 정리

- 사용자 요청 요약: 예약 목록뿐 아니라 관리자 페이지에 들어갈 때마다 지난 예약이 정리되도록 범위를 넓히길 원함.
- 진행한 문제: `/admin` 하위 모든 페이지 진입 시 지난 진행 중 예약을 `처리 완료`로 정리하도록 변경.
- 확인한 원인과 단서: 기존 구현은 `listReservations()` 호출 시에만 실행되어 예약 목록/API 조회 때만 정리됐다.
- 시도한 해결 과정: `app/admin/layout.tsx`를 추가해 관리자 하위 라우트 렌더 전에 `completeExpiredReservations()`를 호출하고, 예약 목록 조회의 중복 호출은 제거했다.
- 적용한 변경 사항: 관리자 레이아웃 자동 정리 추가, `listReservations()`의 자동 정리 호출 제거.
- 검증 결과: `npm run build` 통과, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 여전히 백그라운드 크론은 아니므로 관리자가 관리자 영역에 진입할 때 정리된다.
- 다음 단계: 배포 후 `/admin/settings`, `/admin/menu` 등으로 들어가도 지난 예약이 정리되는지 확인한다.
## 2026-05-26 - 예약 방문 시간 과거 옵션 제거

- 사용자 요청 요약: 당일 예약인데 현재 시각보다 이전 시간까지 방문 시간 선택창에 노출되어 불필요하고 혼란스럽다고 지적.
- 진행한 문제 또는 요청: 예약 시간 옵션을 현재 시간 이후부터 마감 전까지 10분 단위로만 보여주도록 수정.
- 확인한 원인과 단서: 프론트 예약 폼의 `getTimeOptions()`가 영업 시작 시간부터 마감 시간까지 전체 범위를 그대로 생성하고 있었고, 서버 검증도 영업시간 범위만 확인하고 현재 시각 이전 여부는 막지 않았다.
- 시도한 해결 과정: KST 시간 유틸을 프론트/서버에서 공통 사용하게 정리하고, 현재 시각을 10분 단위로 올림 처리해 가장 빠른 예약 가능 시간으로 사용했다.
- 적용한 변경 사항: 예약 폼 시간 옵션은 `max(오픈시간, 현재시각 올림)`부터 `마감 전`까지만 생성하고, 이미 선택한 시간이 시간이 지나 유효하지 않게 되면 자동 초기화되도록 했다. 서버 검증에도 현재 시각 이전 예약 차단을 추가했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 실제 배포 환경에서는 사용자가 페이지를 오래 열어둔 상태에서 30초 단위 갱신 이후 선택값이 사라질 수 있으나, 과거 시간 예약을 막기 위한 의도된 동작이다.
- 다음 단계: 배포 후 현재 시각 기준으로 예약 시간 선택창에서 지난 시간이 사라지는지 모바일에서 확인한다.
## 2026-05-26 - 관리자 임시 비밀번호 보호 추가

- 사용자 요청 요약: 관리자 페이지에 임시 비밀번호 `1234`를 걸고, 나중에는 DB 기반 비밀번호로 제대로 전환하고 싶다고 요청.
- 진행한 문제 또는 요청: `/admin` 하위 화면 진입 전에 비밀번호 입력 화면을 거치도록 구현.
- 확인한 원인과 단서: 현재 관리자 화면은 별도 인증 없이 직접 접근 가능했고, `/api/admin/settings`는 공개 예약 화면에서도 영업시간/휴무/메뉴 상태를 읽기 때문에 API 전체 차단은 적합하지 않았다.
- 시도한 해결 과정: `/admin/*` 화면만 미들웨어로 보호하고, `/admin/login`은 예외로 열어두었다. 로그인 성공 시 `httpOnly` 쿠키를 발급해 관리자 화면 접근을 허용했다.
- 적용한 변경 사항: 관리자 로그인 페이지, 로그인/로그아웃 서버 액션, 관리자 보호 미들웨어, 관리자 내 로그아웃 버튼, `ADMIN_PASSWORD` 환경변수 예시를 추가했다. 환경변수가 없으면 임시값 `1234`를 사용한다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 남은 위험 요소: 현재 방식은 임시 단일 비밀번호 방식이며 사용자별 계정/비밀번호 변경 이력/시도 제한은 없다. 운영 단계에서는 DB 기반 인증과 비밀번호 해시 저장으로 전환해야 한다.
- 다음 단계: 배포 후 `/admin/reservations` 접속 시 `/admin/login`으로 이동하고, `1234` 입력 후 원래 관리자 화면으로 들어가는지 확인한다.
## 2026-05-26 - 상단 관리자 버튼 숨김

- 사용자 요청 요약: PC 상단 네비게이션에 관리자 버튼이 너무 대놓고 보여서 제거하고, 맨 아래의 작은 관리자 링크로만 들어가게 하고 싶다고 요청.
- 진행한 문제 또는 요청: 홈페이지 상단의 관리자 링크를 제거.
- 확인한 원인과 단서: `app/page.tsx` 상단 네비게이션에 `/admin/reservations` 링크가 직접 노출되어 있었고, 모바일 CSS에서는 해당 링크를 숨기는 규칙이 따로 남아 있었다.
- 적용한 변경 사항: 상단 네비게이션의 관리자 링크를 삭제하고, 더 이상 필요 없는 모바일 숨김 CSS를 제거했다. 푸터의 `footer-admin-link`는 유지했다.
- 검증 결과: 빌드/타입 체크 예정.
- 남은 위험 요소: 푸터 관리자 링크는 여전히 접근 가능하며, 직접 `/admin/*` URL 접근은 비밀번호 로그인으로 보호된다.
- 다음 단계: 배포 후 PC 상단에는 메뉴/예약하기만 보이고 하단에서만 관리자 진입이 가능한지 확인한다.
## 2026-05-26 - 실제 위치 주소 반영

- 사용자 요청 요약: 위치를 기존 예시 주소에서 `강원특별자치도 홍천군 홍천읍 홍천로5길 10 1층`으로 변경하고, 이후 위치 관련 정보는 이 주소를 사용해달라고 요청.
- 진행한 문제 또는 요청: 사이트 공통 설정의 주소 값을 실제 주소로 교체.
- 확인한 원인과 단서: 위치 섹션은 `siteConfig.address`를 통해 주소를 표시하고 있어 상수 파일 한 곳만 수정하면 화면 전체에 반영된다.
- 적용한 변경 사항: `lib/constants/site.ts`의 `address` 값을 새 주소로 변경했다.
- 검증 결과: 빌드/타입 체크 예정.
- 남은 위험 요소: 지도 링크나 외부 길찾기 연동은 아직 없으므로, 추후 지도 기능을 추가할 때도 이 주소를 기준으로 사용해야 한다.
- 다음 단계: 배포 후 위치 및 층별 안내 섹션의 주소가 새 주소로 보이는지 확인한다.
## 2026-05-26 - 영업 상태 문구 간소화

- 사용자 요청 요약: 히어로의 영업 상태에서 `오늘 영업시간` 문구가 길어 보여 `영업시간`으로 줄이고 싶다고 요청.
- 적용한 변경 사항: `app/page.tsx`의 영업 상태 상세 문구를 `영업시간 00:00 - 23:50` 형식으로 변경했다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 배포 후 히어로 영업 상태 문구가 간결하게 보이는지 확인한다.
## 2026-05-26 - 예약 섹션 안내 문구 제거 및 중앙 정렬

- 사용자 요청 요약: 예약 섹션의 `방문 시간과 원하는 메뉴를 함께 남겨주세요.`, `단체 방문이나 별도 요청은 요청사항에 적어주세요.` 문구를 삭제하고 방문 예약하기 창을 가운데 정렬하고 싶다고 요청.
- 적용한 변경 사항: 홈 예약 섹션의 안내 문구 블록을 제거하고, 예약 폼 카드가 중앙에 오도록 `cta-layout`을 단일 중앙 정렬 구조로 변경했다. 홈 compact 예약 카드 내부의 보조 문구도 숨겼다.
- 검증 결과: 빌드/타입 체크 예정.
- 다음 단계: 배포 후 예약 섹션이 문구 없이 중앙 카드 중심으로 보이는지 확인한다.
## 2026-05-26 - 메뉴판 섹션 중앙 정렬

- 사용자 요청 요약: 메뉴판 섹션도 방문 예약하기 카드처럼 중앙으로 모으고, 왼쪽 사진 영역을 제거하고 싶다고 요청.
- 적용한 변경 사항: 메뉴 섹션의 왼쪽 메뉴판 사진 figure를 제거하고, 메뉴 목록 컨테이너를 중앙 정렬 및 최대 폭 제한 구조로 변경했다.
- 검증 결과: 빌드/타입 체크 예정.
- 다음 단계: 배포 후 메뉴판 섹션이 한쪽으로 치우치지 않고 중앙에 모여 보이는지 확인한다.
## 2026-05-26 - 방문 예약 카드 확대

- 사용자 요청 요약: 방문 예약하기 예약 창을 현재 크기에서 약 1.8배 크게 키우고 싶다고 요청.
- 적용한 변경 사항: 홈 예약 섹션의 예약 폼 카드 최대 폭을 620px에서 1120px로 확대하고, 해당 카드 내부 여백/제목/입력칸 높이를 함께 키워 넓어진 카드에 맞게 조정했다.
- 검증 결과: 빌드/타입 체크 예정.
- 다음 단계: 배포 후 PC 화면에서 예약 카드가 넓게 보이고, 모바일에서는 화면 폭 안에서 자연스럽게 줄어드는지 확인한다.
## 2026-05-26 - 메뉴판 선택과 예약 장바구니 연결

- 사용자 요청 요약: 메뉴판과 예약 폼 양쪽에서 메뉴를 반복 표시하는 정보 중복을 줄이고, 메뉴판의 HOT/ICE/가격 영역을 클릭하면 예약 폼 장바구니에 담기게 하고 싶다고 요청.
- 진행한 문제 또는 요청: 메뉴판 섹션을 선택 진입점으로 만들고, 예약 폼의 `메뉴 고르기` 영역은 선택된 메뉴만 보여주는 `장바구니`로 변경.
- 확인한 원인과 단서: 기존 예약 폼이 전체 메뉴 목록과 수량 버튼을 다시 렌더링해 메뉴판 섹션과 역할이 겹쳤다.
- 적용한 변경 사항: `MenuOrderBoard` 클라이언트 컴포넌트를 추가해 메뉴 가격 버튼 클릭 이벤트를 발행하고, `ReservationForm`이 이 이벤트를 받아 장바구니 수량을 갱신하도록 연결했다. 예약 폼에서는 전체 메뉴 목록을 제거하고 담긴 메뉴/총 잔 수/총액/수량 조절만 표시한다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과. 임시 서버는 정상 시작했으나 현재 프로젝트와 노드 REPL 환경에 Playwright가 없어 자동 브라우저 클릭 검증은 수행하지 못했다.
- 남은 위험 요소: 실제 모바일 터치감과 메뉴 버튼 줄바꿈은 배포 화면에서 한 번 확인해야 한다.
- 다음 단계: 배포 후 메뉴판의 HOT/ICE/가격 버튼을 눌러 예약 장바구니에 항목이 추가되는지 확인한다.
## 2026-05-26 - 메뉴 섹션 제목 및 메뉴 운영 문구 정리

- 사용자 요청 요약: 메뉴 섹션의 `Menu / 메뉴판` 텍스트를 지우고 중앙에 큰 `MENU`만 보이게 하며, 관리자 메뉴 운영 설명을 예약 화면 기준이 아닌 메뉴판 기준으로 바꾸고 싶다고 요청.
- 적용한 변경 사항: 홈 메뉴 섹션 제목을 중앙 정렬 대문자 `MENU`로 변경했다. 관리자 메뉴 운영 페이지의 안내 문구를 `메뉴판에 노출할 메뉴와 품절 상태를 관리합니다.`로 수정하고, 세부 설명도 메뉴판 기준으로 맞췄다.
- 확인한 동작: 메뉴판은 이미 관리자 설정을 사용해 숨김 메뉴를 제외하고, 품절 메뉴는 가격 버튼 대신 `품절`로 표시하도록 연결되어 있다.
- 검증 결과: `npm run build`, `npx tsc --noEmit` 통과.
- 다음 단계: 배포 후 메뉴 운영에서 숨김/품절을 바꿨을 때 홈 메뉴판에 반영되는지 화면에서 확인한다.
